[package]
name = "uniffi-cs-span-fast-path"
version = "1.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
name = "span_fast_path"

[dependencies]
uniffi = { workspace = true, features = ["build"] }
